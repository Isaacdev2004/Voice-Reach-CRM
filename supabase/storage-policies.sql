-- Create a private bucket named voice-assets in the Supabase dashboard.
-- This starter uses server-side signed upload URLs and the service role client.
-- Do not make voice recordings public.

-- If you later expose storage uploads directly to authenticated browser clients,
-- add policies that only permit access to paths under the authenticated user's ID.
